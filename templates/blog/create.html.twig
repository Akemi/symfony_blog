{% extends 'base.html.twig' %}

{% block title %}New Blog Post{% endblock %}

{% block body %}

    <div class="post">
        {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
        {{ form_widget(form) }}
        {{ form_end(form) }}
    </div>
{% endblock %}

